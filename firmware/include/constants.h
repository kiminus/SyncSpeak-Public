#pragma once

/* Network credentials and server address.
 * Defined in src/secrets.cpp (gitignored).
 * Copy src/secrets.cpp.example → src/secrets.cpp and fill in your values. */
extern const char* WIFI_SSID;
extern const char* WIFI_PASS;

extern const char* SERVER_HOST;
extern const int   SERVER_PORT;
