/* secrets.cpp — gitignored, never commit this file.
 * Copy this file to secrets.cpp and fill in real values. */
#include "constants.h"

const char* WIFI_SSID   = "YourNetworkName";
const char* WIFI_PASS   = "YourPassword";

const char* SERVER_HOST = "192.168.0.0";
const int   SERVER_PORT = 8000;
